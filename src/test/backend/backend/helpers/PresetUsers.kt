package backend.helpers

/** Пользователь из пресета БД ([DatabaseHelper.generatePresetData]) — для тестов, ожидающих сид в БД. */
object PresetUsers {
    const val EMAIL = "user@autotest.com"
    const val PASSWORD = "password"
}
